{
  "name": "worthornot",
  "version": "1.0.0",
  "description": "Singapore HDB Resale Flat Value Detective",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:smoke": "vitest run --config vitest.smoke.config.js",
    "test:smoke-local": "SMOKE_API_URL=http://localhost:3000 vitest run --config vitest.smoke.config.js",
    "setup": "node scripts/run-python.js --setup",
    "download": "node scripts/run-python.js scripts/download_data.py && node scripts/run-python.js scripts/download_ura_data.py",
    "download-hdb": "node scripts/run-python.js scripts/download_data.py",
    "download-ura": "node scripts/run-python.js scripts/download_ura_data.py",
    "deploy:frontend": "npm test && node scripts/bump-version.js && node scripts/deploy-frontend.js && node scripts/indexnow-ping.js",
    "deploy:api": "npm test && fly deploy",
    "deploy:data": "fly machines start e7845746c2d918 && sleep 10 && python3 -c \"import sqlite3; db=sqlite3.connect('server/db/resale.db'); db.execute('PRAGMA wal_checkpoint(TRUNCATE)'); db.close(); print('WAL checkpointed')\" && gzip -kf server/db/resale.db && fly ssh console --command \"rm -f /data/resale.db.new.gz /data/resale.db.new\" && fly ssh sftp put server/db/resale.db.gz /data/resale.db.new.gz && fly ssh console --command \"sh -c 'gunzip -f /data/resale.db.new.gz && mv /data/resale.db.new /data/resale.db && rm -f /data/resale.db-wal /data/resale.db-shm'\" && fly machines restart e7845746c2d918",
    "deploy": "npm test && node scripts/bump-version.js && fly deploy && node scripts/deploy-frontend.js && node scripts/indexnow-ping.js",
    "feedback": "fly ssh console -C \"node /app/scripts/read-feedback.js\"",
    "feedback:local": "node scripts/read-feedback.js server/db/feedback.db"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.1.7"
  }
}
